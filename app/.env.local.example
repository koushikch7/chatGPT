# ===========================================
# ChatGPT Multi-AI App - Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# Run: cp .env.local.example .env.local
#
# SETUP OPTIONS:
# 1. Run interactive setup: ./setup.sh
# 2. Or manually configure below
#
# DEPLOYMENT OPTIONS:
# - Docker MySQL:   docker compose --profile mysql up -d
# - External MySQL: docker compose --profile external up -d
# ===========================================

# ===========================================
# Database Configuration (Required)
# ===========================================
#
# Option A: Docker MySQL (use with --profile mysql)
# DATABASE_URL=mysql://chatgpt:chatgptpassword@mysql:3306/chatgpt
#
# Option B: External MySQL on remote server
# DATABASE_URL=mysql://username:password@your-mysql-host:3306/chatgpt
#
# Option C: MySQL on host machine (accessed from Docker container)
# IMPORTANT: Use 'host.docker.internal' NOT 'localhost' or '127.0.0.1'
# DATABASE_URL=mysql://root:password@host.docker.internal:3306/chatgpt
#
DATABASE_URL=mysql://chatgpt:chatgptpassword@mysql:3306/chatgpt

# MySQL Docker container settings (only needed with --profile mysql)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=chatgpt
MYSQL_USER=chatgpt
MYSQL_PASSWORD=chatgptpassword

# ===========================================
# NextAuth Configuration (Required)
# ===========================================
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Your app URL (change for production)
# For local development: http://localhost:3000
# For production with nginx proxy: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# ===========================================
# Google OAuth (Optional - for Google SSO)
# ===========================================
# Get from: https://console.cloud.google.com/apis/credentials
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===========================================
# GitHub OAuth (Optional - for GitHub SSO)
# ===========================================
# Get from: https://github.com/settings/developers
# Callback URL: {NEXTAUTH_URL}/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=